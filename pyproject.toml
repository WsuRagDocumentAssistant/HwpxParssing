[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hwpx_parser"
version = "0.1.0"
description = "this is hwpx parser"
readme = "README.md"
requires-python = ">=3.11"